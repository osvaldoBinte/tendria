class ValidateCouponsEntity {
  final bool isValid;
  final String description;
  final int bonusPoints;

  ValidateCouponsEntity({
    required this.isValid,
    required this.description,
    required this.bonusPoints,
  });
}