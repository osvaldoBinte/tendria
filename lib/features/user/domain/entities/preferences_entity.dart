class PreferencesEntity {
  final int? agemin;
  final int? agemax;
  final String? searchgender;
  final String ? connectiontype;

  PreferencesEntity({
     this.agemin,
     this.agemax,
     this.searchgender,
     this.connectiontype,
  });
}