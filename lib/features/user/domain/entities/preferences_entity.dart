class PreferencesEntity {
  final int? agemin;
  final int? agemax;
  final double? distancekm;
  final String? searchgender;
  final String ? connectiontype;

  PreferencesEntity({
     this.agemin,
     this.agemax,
      this.distancekm,
     this.searchgender,
     this.connectiontype,
  });
}